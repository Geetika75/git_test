# git_test

### create a repo in github
### copy the ssh link in clone
### in cmd to clone the repo to computer - git clone sshlinlk(paste the sshlink)
### after edit, to stage the files - git add .
### to commit - git commit -m "msg"
### to push - git push
### git remote -v
### git status
### git log